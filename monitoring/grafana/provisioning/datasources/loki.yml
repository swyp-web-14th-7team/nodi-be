# Grafana 기동 시 Loki 데이터소스를 자동 등록 (UI 에서 수동 추가 불필요)
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    # compose 내부 네트워크의 서비스명. Loki 는 호스트에 노출돼 있지 않음.
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000